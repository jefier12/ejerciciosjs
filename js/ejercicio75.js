// Crea una función que reciba dos números como parámetros y devuelva el mayor de
// ellos.

function mayorDeDos(num1, num2) {
    return num1 > num2 ? num1 : num2;
  }
  console.log(mayorDeDos(5, 8));
  